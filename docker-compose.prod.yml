# ============================================
# 说明：此文件已废弃。
#
# 端口安全绑定（127.0.0.1）已直接合并进 docker-compose.yml，
# 服务器与本地统一使用：docker compose up -d
#
# 生产环境入口：宿主机 Nginx 监听 80/443 → 反代 http://127.0.0.1:8080
# ============================================
